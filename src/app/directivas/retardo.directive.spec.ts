import { RetardoDirective } from './retardo.directive';

describe('RetardoDirective', () => {
  it('should create an instance', () => {
    const directive = new RetardoDirective();
    expect(directive).toBeTruthy();
  });
});
